{
  "name": "minimal-es6-wasm",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react-scripts": "1.0.17"
  },
  "scripts": {
    "start": "react-scripts start",
    "build-wasm": "cd wasm && make clean && make all",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
